export interface VacationRequest {
    date_from: string;
    date_to: string;
    date_req: string;
    date_change: string;
    year_period: string;
    order_type_is_vacation: boolean,
    order_type_is_change: boolean,
}